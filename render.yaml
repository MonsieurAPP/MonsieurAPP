services:
  - type: web
    name: monsieurapp
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: /bin/sh -c 'exec uvicorn app.main:app --host 0.0.0.0 --port "${PORT:-10000}"'
    plan: starter
    numInstances: 1
    autoDeployTrigger: commit
    healthCheckPath: /health
    envVars:
      - key: APP_DATA_DIR
        value: /app/persisted-data
      - key: AI_API_KEY
        sync: false
      - key: AI_MODEL
        sync: false
      - key: AI_BASE_URL
        sync: false
    disk:
      name: app-data
      mountPath: /app/persisted-data
      sizeGB: 1
